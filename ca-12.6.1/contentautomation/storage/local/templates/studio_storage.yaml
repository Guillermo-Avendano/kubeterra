apiVersion: v1
kind: PersistentVolume
metadata:
  name: studio-pv-storage
  labels:
    type: studio-pv-storage
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: "<STUDIO_PV_VOLUME>"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: studio-log-pv-claim
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 4Gi
  selector: 
    matchLabels: 
      type: studio-pv-storage    
